package sciapi.api.basis.bunch.bunchreal;

/**
 * The type of the bunch which is identified by integer id.
 * Same type must have same id.
 * */
public interface IBunchRealIdType extends IBunchRealType {
	public int getId();
}
